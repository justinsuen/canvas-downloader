# Frontend Environment Configuration
# Copy this file to .env and update values for your environment

# Environment (development, production)
REACT_APP_NODE_ENV=development

# API Configuration
# Option 1: Full API URL (recommended for production)
# REACT_APP_API_URL=https://api.yourdomain.com

# Option 2: API Host and Port (alternative approach)
# For development, these default to localhost:8000
REACT_APP_API_HOST=localhost
REACT_APP_API_PORT=8000

# Production Examples:
# REACT_APP_API_HOST=api.yourdomain.com
# REACT_APP_API_PORT=443

# Notes:
# - In production, HTTPS is automatically used
# - In development, HTTP is used with localhost
# - If REACT_APP_API_URL is set, it takes precedence over HOST/PORT
# - Port 443 will be omitted from URLs automatically